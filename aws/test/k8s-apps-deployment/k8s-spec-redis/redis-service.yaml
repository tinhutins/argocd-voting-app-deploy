apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: vote
spec:
  type: ExternalName
  externalName: riftdevops-test-redis.vqnhks.ng.0001.use1.cache.amazonaws.com
  ports:
    - name: redis
      port: 6379
      targetPort: 6379

# apiVersion: v1
# kind: Service
# metadata:
#   labels:
#     app: redis
#   name: redis
#   namespace: vote
# spec:
#   type: ClusterIP
#   ports:
#   - name: "redis-service"
#     port: 6379
#     targetPort: 6379
#   selector:
#     app: redis
  
