apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: vote
spec:
  type: ExternalName
  # UPDATE after Step 3 — replace with Azure Flexible Server FQDN
  # e.g. riftdevops-test-pg.postgres.database.azure.com
  externalName: riftdevops-test-pg.postgres.database.azure.com
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
